{{- $name := .Values.clientLogsPvc.name | default "client-logs-pvc" -}}
{{- $storage := .Values.clientLogsPvc.storage | default "10Gi" -}}
{{- if .Values.hostPath.enabled }}
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ $name }}
  labels:
    {{- include "tracebloc.labels" . | nindent 4 }}
spec:
  storageClassName: {{ .Values.storageClass.name }}
  capacity:
    storage: {{ $storage }}
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: {{ required "hostPath.logsPath is required when hostPath.enabled" .Values.hostPath.logsPath }}
---
{{- end }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ $name }}
  namespace: {{ .Release.Namespace }}
  annotations:
    "helm.sh/resource-policy": keep
  labels:
    {{- include "tracebloc.labels" . | nindent 4 }}
spec:
  storageClassName: {{ .Values.storageClass.name }}
  accessModes:
    - {{ .Values.pvcAccessMode | default "ReadWriteMany" }}
  resources:
    requests:
      storage: {{ $storage }}
