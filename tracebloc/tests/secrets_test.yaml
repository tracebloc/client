suite: Secrets
templates:
  - templates/secrets.yaml
  - templates/docker-registry-secret.yaml
tests:
  - it: should create tracebloc secret with encoded password
    template: templates/secrets.yaml
    set:
      secrets:
        create: true
        clientPassword: "my-secret-pass"
    asserts:
      - isKind:
          of: Secret
      - equal:
          path: metadata.name
          value: RELEASE-NAME-secrets
      - equal:
          path: type
          value: Opaque
      - isNotEmpty:
          path: data.CLIENT_PASSWORD
      - matchRegex:
          path: metadata.labels["app.kubernetes.io/managed-by"]
          pattern: Helm

  - it: should not create secret when create is false
    template: templates/secrets.yaml
    set:
      secrets:
        create: false
        clientPassword: "unused"
    asserts:
      - hasDocuments:
          count: 0

  - it: should create docker registry secret
    template: templates/docker-registry-secret.yaml
    set:
      dockerRegistry:
        create: true
        server: https://index.docker.io/v1/
        username: testuser
        password: testpass
        email: test@test.com
    asserts:
      - isKind:
          of: Secret
      - equal:
          path: metadata.name
          value: RELEASE-NAME-regcred
      - equal:
          path: type
          value: kubernetes.io/dockerconfigjson
      - isNotEmpty:
          path: data[".dockerconfigjson"]

  - it: should not create docker registry secret when create is false
    template: templates/docker-registry-secret.yaml
    set:
      dockerRegistry:
        create: false
    asserts:
      - hasDocuments:
          count: 0
