# EXPECTED QoS CLASS per workload — CSI cluster (hostPath.enabled=false: EKS/AKS/
# OpenShift). Asserted separately from the hostPath file because the MODE CHANGES
# THE ANSWER: jobs-manager renders no init container here, so cpu req != lim becomes
# its sole remaining blocker. A single-mode check would have agreed with the wrong
# comments on exactly the clusters the installer provisions (backend#2872).
class  mysql-client                 Burstable
class  t-auto-upgrade               Burstable
class  t-egress-proxy               Burstable
class  t-egress-reachability-check  Burstable
class  t-image-refresh              Burstable
class  t-jobs-manager               Burstable
class  t-requests-proxy             Burstable
class  t-resource-monitor           Burstable
class  t-storage-assertions-check   Burstable
class  t-telemetry-collector        Burstable

# jobs-manager drops off this list entirely — that IS the difference between the two
# modes. mysql keeps mysql-format-guard unconditionally, which is why mysql can never
# be Guaranteed on any cluster whatever `resources.mysql.limits.cpu` is set to.
init   mysql-client                 mysql-format-guard
