# EXPECTED QoS CLASS per workload — egress lockdown on (hostPath.enabled=true).
#
# THE MODE EXISTS FOR ONE WORKLOAD. `egress-enforcement-check` renders only with
# networkPolicy.training.enabled=true, allowExternalHttps=false AND a probe host
# set, so no other mode in this suite produced it and the Job was never classified
# (Bugbot, review on client#922). Stripping its resources would demote it to
# BestEffort with the whole suite still green — the hole the GPU expect files were
# added to close, in a second place.
#
# Compared by SET EQUALITY in both directions, like every other mode here: a new
# workload with no row FAILS, and a row the chart no longer renders FAILS.
class  mysql-client                  Burstable
class  t-auto-upgrade                Guaranteed
class  t-egress-enforcement-check    Burstable
class  t-egress-proxy                Burstable
class  t-egress-reachability-check   Burstable
class  t-image-refresh               Guaranteed
class  t-jobs-manager                Burstable
class  t-requests-proxy              Burstable
class  t-resource-monitor            Burstable
class  t-storage-assertions-check    Burstable
class  t-telemetry-collector         Burstable

# The unresourced init containers that make the two above un-Guaranteeable. Same
# rows as every other hostPath mode: the lockdown changes what RENDERS, not how
# the existing workloads are resourced.
init   t-jobs-manager                init-writable-data
init   mysql-client                  init-mysql-data,mysql-format-guard
